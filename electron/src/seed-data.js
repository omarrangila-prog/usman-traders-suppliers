// GENERATED from db.py by tools/gen-seed.py - do not edit by hand.

export const SCHEMA = "\nCREATE TABLE IF NOT EXISTS users (\n    id            INTEGER PRIMARY KEY AUTOINCREMENT,\n    username      TEXT NOT NULL UNIQUE,\n    full_name     TEXT NOT NULL DEFAULT '',\n    role          TEXT NOT NULL DEFAULT 'staff',\n    password_hash TEXT NOT NULL,\n    salt          TEXT NOT NULL,\n    active        INTEGER NOT NULL DEFAULT 1,\n    created_at    TEXT NOT NULL DEFAULT (datetime('now'))\n);\n\nCREATE TABLE IF NOT EXISTS settings (\n    key   TEXT PRIMARY KEY,\n    value TEXT NOT NULL\n);\n\nCREATE TABLE IF NOT EXISTS company (\n    id       INTEGER PRIMARY KEY CHECK (id = 1),\n    name     TEXT NOT NULL DEFAULT 'My Trading Company',\n    tagline  TEXT NOT NULL DEFAULT '',\n    logo     TEXT NOT NULL DEFAULT '',\n    address  TEXT NOT NULL DEFAULT '',\n    city     TEXT NOT NULL DEFAULT '',\n    phone    TEXT NOT NULL DEFAULT '',\n    email    TEXT NOT NULL DEFAULT '',\n    website  TEXT NOT NULL DEFAULT '',\n    tax_id   TEXT NOT NULL DEFAULT '',\n    currency TEXT NOT NULL DEFAULT 'PKR',\n    tax_rate REAL NOT NULL DEFAULT 0,\n    footer   TEXT NOT NULL DEFAULT 'Thank you for your business.'\n);\n\nCREATE TABLE IF NOT EXISTS customers (\n    id         INTEGER PRIMARY KEY AUTOINCREMENT,\n    code       TEXT NOT NULL DEFAULT '',\n    name       TEXT NOT NULL,\n    contact    TEXT NOT NULL DEFAULT '',\n    phone      TEXT NOT NULL DEFAULT '',\n    email      TEXT NOT NULL DEFAULT '',\n    address    TEXT NOT NULL DEFAULT '',\n    city       TEXT NOT NULL DEFAULT '',\n    tax_id     TEXT NOT NULL DEFAULT '',\n    notes      TEXT NOT NULL DEFAULT '',\n    active     INTEGER NOT NULL DEFAULT 1,\n    created_at TEXT NOT NULL DEFAULT (datetime('now'))\n);\n\nCREATE TABLE IF NOT EXISTS suppliers (\n    id         INTEGER PRIMARY KEY AUTOINCREMENT,\n    code       TEXT NOT NULL DEFAULT '',\n    name       TEXT NOT NULL,\n    contact    TEXT NOT NULL DEFAULT '',\n    phone      TEXT NOT NULL DEFAULT '',\n    email      TEXT NOT NULL DEFAULT '',\n    address    TEXT NOT NULL DEFAULT '',\n    city       TEXT NOT NULL DEFAULT '',\n    tax_id     TEXT NOT NULL DEFAULT '',\n    notes      TEXT NOT NULL DEFAULT '',\n    active     INTEGER NOT NULL DEFAULT 1,\n    created_at TEXT NOT NULL DEFAULT (datetime('now'))\n);\n\nCREATE TABLE IF NOT EXISTS products (\n    id             INTEGER PRIMARY KEY AUTOINCREMENT,\n    sku            TEXT NOT NULL UNIQUE,\n    name           TEXT NOT NULL,\n    category       TEXT NOT NULL DEFAULT '',\n    unit           TEXT NOT NULL DEFAULT 'pcs',\n    pack_size      TEXT NOT NULL DEFAULT '',\n    purchase_price REAL NOT NULL DEFAULT 0,\n    sale_price     REAL NOT NULL DEFAULT 0,\n    stock          REAL NOT NULL DEFAULT 0,\n    reorder_level  REAL NOT NULL DEFAULT 0,\n    supplier_id    INTEGER REFERENCES suppliers(id) ON DELETE SET NULL,\n    notes          TEXT NOT NULL DEFAULT '',\n    active         INTEGER NOT NULL DEFAULT 1,\n    created_at     TEXT NOT NULL DEFAULT (datetime('now'))\n);\n\nCREATE TABLE IF NOT EXISTS orders (\n    id              INTEGER PRIMARY KEY AUTOINCREMENT,\n    order_no        TEXT NOT NULL UNIQUE,\n    customer_id     INTEGER NOT NULL REFERENCES customers(id),\n    order_date      TEXT NOT NULL,\n    delivery_date   TEXT NOT NULL DEFAULT '',\n    status          TEXT NOT NULL DEFAULT 'Pending',\n    delivery_status TEXT NOT NULL DEFAULT 'Not Dispatched',\n    tracking_note   TEXT NOT NULL DEFAULT '',\n    notes           TEXT NOT NULL DEFAULT '',\n    subtotal        REAL NOT NULL DEFAULT 0,\n    discount        REAL NOT NULL DEFAULT 0,\n    tax             REAL NOT NULL DEFAULT 0,\n    total           REAL NOT NULL DEFAULT 0,\n    stock_applied   INTEGER NOT NULL DEFAULT 0,\n    created_at      TEXT NOT NULL DEFAULT (datetime('now'))\n);\n\nCREATE TABLE IF NOT EXISTS order_items (\n    id         INTEGER PRIMARY KEY AUTOINCREMENT,\n    order_id   INTEGER NOT NULL REFERENCES orders(id) ON DELETE CASCADE,\n    product_id INTEGER NOT NULL REFERENCES products(id),\n    qty        REAL NOT NULL DEFAULT 0,\n    price      REAL NOT NULL DEFAULT 0,\n    line_total REAL NOT NULL DEFAULT 0\n);\n\nCREATE TABLE IF NOT EXISTS invoices (\n    id           INTEGER PRIMARY KEY AUTOINCREMENT,\n    invoice_no   TEXT NOT NULL UNIQUE,\n    order_id     INTEGER REFERENCES orders(id) ON DELETE SET NULL,\n    customer_id  INTEGER NOT NULL REFERENCES customers(id),\n    invoice_date TEXT NOT NULL,\n    due_date     TEXT NOT NULL DEFAULT '',\n    subtotal     REAL NOT NULL DEFAULT 0,\n    discount     REAL NOT NULL DEFAULT 0,\n    tax          REAL NOT NULL DEFAULT 0,\n    total        REAL NOT NULL DEFAULT 0,\n    paid         REAL NOT NULL DEFAULT 0,\n    status       TEXT NOT NULL DEFAULT 'Unpaid',\n    notes        TEXT NOT NULL DEFAULT '',\n    created_at   TEXT NOT NULL DEFAULT (datetime('now'))\n);\n\nCREATE TABLE IF NOT EXISTS invoice_items (\n    id         INTEGER PRIMARY KEY AUTOINCREMENT,\n    invoice_id INTEGER NOT NULL REFERENCES invoices(id) ON DELETE CASCADE,\n    product_id INTEGER NOT NULL REFERENCES products(id),\n    qty        REAL NOT NULL DEFAULT 0,\n    price      REAL NOT NULL DEFAULT 0,\n    line_total REAL NOT NULL DEFAULT 0\n);\n\nCREATE TABLE IF NOT EXISTS purchases (\n    id            INTEGER PRIMARY KEY AUTOINCREMENT,\n    purchase_no   TEXT NOT NULL UNIQUE,\n    supplier_id   INTEGER NOT NULL REFERENCES suppliers(id),\n    purchase_date TEXT NOT NULL,\n    bill_no       TEXT NOT NULL DEFAULT '',\n    subtotal      REAL NOT NULL DEFAULT 0,\n    discount      REAL NOT NULL DEFAULT 0,\n    tax           REAL NOT NULL DEFAULT 0,\n    total         REAL NOT NULL DEFAULT 0,\n    paid          REAL NOT NULL DEFAULT 0,\n    status        TEXT NOT NULL DEFAULT 'Received',\n    notes         TEXT NOT NULL DEFAULT '',\n    stock_applied INTEGER NOT NULL DEFAULT 0,\n    created_at    TEXT NOT NULL DEFAULT (datetime('now'))\n);\n\nCREATE TABLE IF NOT EXISTS purchase_items (\n    id          INTEGER PRIMARY KEY AUTOINCREMENT,\n    purchase_id INTEGER NOT NULL REFERENCES purchases(id) ON DELETE CASCADE,\n    product_id  INTEGER NOT NULL REFERENCES products(id),\n    qty         REAL NOT NULL DEFAULT 0,\n    price       REAL NOT NULL DEFAULT 0,\n    line_total  REAL NOT NULL DEFAULT 0\n);\n\n-- Entries captured in the field, possibly with no connection. They are held on\n-- the phone until it can reach the server, so client_id is generated by the\n-- device and kept unique: a retry after a reply went missing cannot duplicate\n-- the booking. They stay separate from the ledger until reviewed and converted.\nCREATE TABLE IF NOT EXISTS field_entries (\n    id          INTEGER PRIMARY KEY AUTOINCREMENT,\n    client_id   TEXT NOT NULL UNIQUE,\n    kind        TEXT NOT NULL DEFAULT 'Booking',\n    party_name  TEXT NOT NULL DEFAULT '',\n    phone       TEXT NOT NULL DEFAULT '',\n    city        TEXT NOT NULL DEFAULT '',\n    entry_date  TEXT NOT NULL DEFAULT '',\n    notes       TEXT NOT NULL DEFAULT '',\n    items       TEXT NOT NULL DEFAULT '[]',\n    total       REAL NOT NULL DEFAULT 0,\n    device      TEXT NOT NULL DEFAULT '',\n    captured_at TEXT NOT NULL DEFAULT '',\n    status      TEXT NOT NULL DEFAULT 'Pending',\n    linked_id   INTEGER,\n    linked_no   TEXT NOT NULL DEFAULT '',\n    created_at  TEXT NOT NULL DEFAULT (datetime('now'))\n);\n\n-- Double-entry bookkeeping. Every financial event writes a balanced journal\n-- entry, and the statements are derived from those entries rather than\n-- recalculated from documents - so the books can always be traced and proved.\nCREATE TABLE IF NOT EXISTS accounts (\n    id       INTEGER PRIMARY KEY AUTOINCREMENT,\n    code     TEXT NOT NULL UNIQUE,\n    name     TEXT NOT NULL,\n    type     TEXT NOT NULL,               -- Asset, Liability, Equity, Income, Expense\n    subtype  TEXT NOT NULL DEFAULT '',\n    is_cash  INTEGER NOT NULL DEFAULT 0,  -- cash and bank, for the cash summary\n    system   INTEGER NOT NULL DEFAULT 0,  -- posted to automatically; do not delete\n    active   INTEGER NOT NULL DEFAULT 1,\n    notes    TEXT NOT NULL DEFAULT ''\n);\n\nCREATE TABLE IF NOT EXISTS journal_entries (\n    id         INTEGER PRIMARY KEY AUTOINCREMENT,\n    entry_no   TEXT NOT NULL,\n    entry_date TEXT NOT NULL,\n    memo       TEXT NOT NULL DEFAULT '',\n    source     TEXT NOT NULL DEFAULT 'Manual',\n    source_id  INTEGER,\n    created_at TEXT NOT NULL DEFAULT (datetime('now'))\n);\n\nCREATE TABLE IF NOT EXISTS journal_lines (\n    id           INTEGER PRIMARY KEY AUTOINCREMENT,\n    entry_id     INTEGER NOT NULL REFERENCES journal_entries(id) ON DELETE CASCADE,\n    account_id   INTEGER NOT NULL REFERENCES accounts(id),\n    debit        REAL NOT NULL DEFAULT 0,\n    credit       REAL NOT NULL DEFAULT 0,\n    memo         TEXT NOT NULL DEFAULT '',\n    -- ticked off against a bank statement during reconciliation\n    cleared      INTEGER NOT NULL DEFAULT 0,\n    cleared_date TEXT NOT NULL DEFAULT ''\n);\n\n-- Each year-end close records how far the books were closed and the entry\n-- that swept profit into retained earnings, so it can be traced or undone.\nCREATE TABLE IF NOT EXISTS closings (\n    id         INTEGER PRIMARY KEY AUTOINCREMENT,\n    closed_to  TEXT NOT NULL,\n    entry_id   INTEGER REFERENCES journal_entries(id) ON DELETE SET NULL,\n    net_profit REAL NOT NULL DEFAULT 0,\n    created_at TEXT NOT NULL DEFAULT (datetime('now'))\n);\n\n-- Straight-line depreciation: cost spread evenly over a life in months.\nCREATE TABLE IF NOT EXISTS fixed_assets (\n    id            INTEGER PRIMARY KEY AUTOINCREMENT,\n    name          TEXT NOT NULL,\n    purchase_date TEXT NOT NULL,\n    cost          REAL NOT NULL DEFAULT 0,\n    salvage       REAL NOT NULL DEFAULT 0,\n    life_months   INTEGER NOT NULL DEFAULT 60,\n    asset_account TEXT NOT NULL DEFAULT '1500',\n    expense_account TEXT NOT NULL DEFAULT '6800',\n    depreciated_to TEXT NOT NULL DEFAULT '',\n    active        INTEGER NOT NULL DEFAULT 1\n);\n\nCREATE INDEX IF NOT EXISTS idx_journal_lines_entry ON journal_lines(entry_id);\nCREATE INDEX IF NOT EXISTS idx_journal_lines_account ON journal_lines(account_id);\nCREATE INDEX IF NOT EXISTS idx_journal_source ON journal_entries(source, source_id);\n\nCREATE TABLE IF NOT EXISTS stock_moves (\n    id         INTEGER PRIMARY KEY AUTOINCREMENT,\n    product_id INTEGER NOT NULL REFERENCES products(id) ON DELETE CASCADE,\n    move_date  TEXT NOT NULL DEFAULT (datetime('now')),\n    kind       TEXT NOT NULL,\n    qty        REAL NOT NULL,\n    balance    REAL NOT NULL DEFAULT 0,\n    reference  TEXT NOT NULL DEFAULT '',\n    note       TEXT NOT NULL DEFAULT ''\n);\n\nCREATE INDEX IF NOT EXISTS idx_order_items_order ON order_items(order_id);\nCREATE INDEX IF NOT EXISTS idx_invoice_items_invoice ON invoice_items(invoice_id);\nCREATE INDEX IF NOT EXISTS idx_purchase_items_purchase ON purchase_items(purchase_id);\nCREATE INDEX IF NOT EXISTS idx_stock_moves_product ON stock_moves(product_id);\nCREATE INDEX IF NOT EXISTS idx_orders_customer ON orders(customer_id);\nCREATE INDEX IF NOT EXISTS idx_invoices_customer ON invoices(customer_id);\nCREATE INDEX IF NOT EXISTS idx_purchases_supplier ON purchases(supplier_id);\n\n-- ------------------------------------------------------------------------\n-- Sharing data between the office desktop and the cloud.\n--\n-- Two machines both numbering their rows from 1 cannot be merged, so every\n-- record that travels carries a uid that is unique everywhere. Rows created by\n-- a person get a random one; rows that both sides seed for themselves - the\n-- item master, the vendor list, the chart of accounts - derive theirs from the\n-- business key, so the two sides recognise them as the same row instead of\n-- ending up with two of everything.\n-- ------------------------------------------------------------------------\n\nCREATE TABLE IF NOT EXISTS sync_state (\n    key   TEXT PRIMARY KEY,\n    value TEXT NOT NULL\n);\n\n-- A row that is simply gone cannot be told apart from one that has not\n-- arrived yet, so deletions leave a marker behind for the other side.\nCREATE TABLE IF NOT EXISTS tombstones (\n    entity     TEXT NOT NULL,\n    uid        TEXT NOT NULL,\n    deleted_at TEXT NOT NULL,\n    PRIMARY KEY (entity, uid)\n);\n\n-- When both sides changed the same record since they last spoke, the newer one\n-- wins - but the other is kept here rather than thrown away, so nothing is\n-- lost silently and someone can look at what happened.\nCREATE TABLE IF NOT EXISTS sync_conflicts (\n    id         INTEGER PRIMARY KEY AUTOINCREMENT,\n    entity     TEXT NOT NULL,\n    uid        TEXT NOT NULL,\n    noticed_at TEXT NOT NULL,\n    kept       TEXT NOT NULL,\n    discarded  TEXT NOT NULL,\n    reviewed   INTEGER NOT NULL DEFAULT 0\n);\n\nCREATE INDEX IF NOT EXISTS idx_tombstones_time ON tombstones(deleted_at);\n\n-- What we believe the other side already has. Comparing the current rows\n-- against this is how a change is noticed, rather than trusting every one of\n-- the seventy-odd write paths to remember to mark the row as changed. A path\n-- that forgot would lose the change silently, and this cannot: if the content\n-- differs from what was last agreed, it is a change, whoever made it and\n-- however they made it.\nCREATE TABLE IF NOT EXISTS sync_shadow (\n    entity    TEXT NOT NULL,\n    uid       TEXT NOT NULL,\n    hash      TEXT NOT NULL,\n    synced_at TEXT NOT NULL,\n    PRIMARY KEY (entity, uid)\n);\n";

export const SCHEMA_VERSION = "6";

// sku, name, category, unit, pack_size, purchase_price, sale_price, stock, reorder_level
export const SEED_PRODUCTS = [["00001", "ACHAR SACHET RS;10", "Achar & Chatni", "pcs", "Sachet", 0, 10, 0, 0], ["00002", "ACHAR 5KG BUCKET", "Achar & Chatni", "bucket", "5 kg", 0, 0, 0, 0], ["00003", "ACHAR 10KG BUCKET", "Achar & Chatni", "bucket", "10 kg", 0, 0, 0, 0], ["00004", "ACHAR 20 KG BUCKET", "Achar & Chatni", "bucket", "20 kg", 0, 0, 0, 0], ["00005", "HARI MIRCH POWDER RS 10", "Masala & Spices", "pcs", "Sachet", 0, 10, 0, 0], ["00006", "BIRYANI MASALA RS 10", "Masala & Spices", "pcs", "Sachet", 0, 10, 0, 0], ["00007", "PULLAO RS 10", "Masala & Spices", "pcs", "Sachet", 0, 10, 0, 0], ["00008", "HALDI POWDER RS 10", "Masala & Spices", "pcs", "Sachet", 0, 10, 0, 0], ["00009", "CHAT MASALA RS 10", "Masala & Spices", "pcs", "Sachet", 0, 10, 0, 0], ["00010", "FISH MASALA RS 10", "Masala & Spices", "pcs", "Sachet", 0, 10, 0, 0], ["00011", "ACHAR GHOSHT RS 10", "Masala & Spices", "pcs", "Sachet", 0, 10, 0, 0], ["00012", "TIKKA MASALA RS 10", "Masala & Spices", "pcs", "Sachet", 0, 10, 0, 0], ["00013", "DAL SABZI MASALA RS 10", "Masala & Spices", "pcs", "Sachet", 0, 10, 0, 0], ["00014", "QUORMA MASALA RS 10", "Masala & Spices", "pcs", "Sachet", 0, 10, 0, 0], ["00015", "KARAHI GHOSHT MASALA RS 10", "Masala & Spices", "pcs", "Sachet", 0, 10, 0, 0], ["00016", "DHANIA POWDER RS 10", "Masala & Spices", "pcs", "Sachet", 0, 10, 0, 0], ["00017", "G/MASALA POWDER RS 20", "Masala & Spices", "pcs", "Sachet", 0, 20, 0, 0], ["00018", "SALAN MASALA RS 10", "Masala & Spices", "pcs", "Sachet", 0, 10, 0, 0], ["00019", "G/MASALA POWDER RS 10", "Masala & Spices", "pcs", "Sachet", 0, 10, 0, 0], ["00020", "NEEL RS 20", "Household", "pcs", "Sachet", 0, 20, 0, 0], ["00021", "NEEL RS 30", "Household", "pcs", "Sachet", 0, 30, 0, 0], ["00022", "NEEL RS 50", "Household", "pcs", "Sachet", 0, 50, 0, 0], ["00023", "SARSON OIL RS;10", "Oils", "pcs", "Sachet", 0, 10, 0, 0], ["00024", "SARSON OIL RS;20", "Oils", "pcs", "Sachet", 0, 20, 0, 0], ["00025", "COCONUT OIL RS;20", "Oils", "pcs", "Sachet", 0, 20, 0, 0], ["00026", "CAANDIL RS;10", "Household", "pcs", "Pack", 0, 10, 0, 0], ["00027", "CAANDIL RS;20", "Household", "pcs", "Pack", 0, 20, 0, 0], ["00028", "CHILLI SAUCE 750 ML", "Sauces & Vinegar", "bottle", "750 ml", 0, 0, 0, 0], ["00029", "SOYA SAUCE 750 ML", "Sauces & Vinegar", "bottle", "750 ml", 0, 0, 0, 0], ["00030", "PLAIN VENGAR 750 ML", "Sauces & Vinegar", "bottle", "750 ml", 0, 0, 0, 0], ["00031", "SYRUP ICE CREAM SODA 750 ML", "Syrups", "bottle", "750 ml", 0, 0, 0, 0], ["00032", "SYRUP RED 750 ML", "Syrups", "bottle", "750 ml", 0, 0, 0, 0], ["00033", "SYRUP FALSA 750 ML", "Syrups", "bottle", "750 ml", 0, 0, 0, 0], ["00034", "SYRUP PINE APPLE 750 ML", "Syrups", "bottle", "750 ml", 0, 0, 0, 0], ["00035", "SYRUP MANGO 750 ML", "Syrups", "bottle", "750 ml", 0, 0, 0, 0], ["00036", "SYRUP STRAWBERRY 750 ML", "Syrups", "bottle", "750 ml", 0, 0, 0, 0], ["00037", "SYRUP LEECHI 750 ML", "Syrups", "bottle", "750 ml", 0, 0, 0, 0], ["00038", "SYRUP BLUEBERRYI 750 ML", "Syrups", "bottle", "750 ml", 0, 0, 0, 0], ["00039", "SYRUP PEACH 750 ML", "Syrups", "bottle", "750 ml", 0, 0, 0, 0], ["00040", "SAVHET RED CHILLI POWDER", "Masala & Spices", "pcs", "Sachet", 0, 0, 0, 0], ["00041", "HAND WASH 500ML ALOEVERA", "Household", "bottle", "500 ml", 0, 0, 0, 0], ["00042", "HAND WASH 500ML COCONUT", "Household", "bottle", "500 ml", 0, 0, 0, 0], ["00043", "HAND WASH 500ML LAVENDER", "Household", "bottle", "500 ml", 0, 0, 0, 0], ["00044", "HAND WASH 500ML ROSE", "Household", "bottle", "500 ml", 0, 0, 0, 0], ["00045", "CHATNI POODINA", "Achar & Chatni", "pcs", "", 0, 0, 0, 0], ["00046", "QUORMA MASALA RS 20", "Masala & Spices", "pcs", "Sachet", 0, 20, 0, 0], ["00047", "SARSO OIL RS 60", "Oils", "pcs", "Bottle", 0, 60, 0, 0], ["00048", "COCNUT OIL RS60", "Oils", "pcs", "Bottle", 0, 60, 0, 0], ["00049", "SALAN MASALA RS 20", "Masala & Spices", "pcs", "Sachet", 0, 20, 0, 0], ["000049", "KARAHAI GOSHT RS20", "Masala & Spices", "pcs", "Sachet", 0, 20, 0, 0], ["00050", "MURGI MASALA RS 20", "Masala & Spices", "pcs", "Sachet", 0, 20, 0, 0], ["00051", "RED CHILLI CUTTER", "Masala & Spices", "pcs", "", 0, 0, 0, 0], ["00052", "RED CHILLI POWDER 1KG", "Masala & Spices", "pack", "1 kg", 0, 0, 0, 0], ["00053", "DHANIYA POWDER 1KG", "Masala & Spices", "pack", "1 kg", 0, 0, 0, 0], ["00054", "HALDI POWDER 1KG", "Masala & Spices", "pack", "1 kg", 0, 0, 0, 0], ["00055", "RED CHILLI CUTTER 1KG", "Masala & Spices", "pack", "1 kg", 0, 0, 0, 0], ["00056", "MAMTA TEA SACHET RS20", "Tea", "pcs", "Sachet", 0, 20, 0, 0], ["00057", "SINDHRI ACHAR SACHET RS 10", "Achar & Chatni", "pcs", "Sachet", 0, 10, 0, 0], ["00058", "MAMTA TEA SACHET RS;10", "Tea", "pcs", "Sachet", 0, 10, 0, 0], ["00059", "TEA MIXTURE 5KG JAR", "Tea", "jar", "5 kg", 0, 0, 0, 0], ["00060", "AJEENO MOTO SACHET RS10", "Masala & Spices", "pcs", "Sachet", 0, 10, 0, 0], ["00061", "ZEERA 1KG", "Masala & Spices", "pack", "1 kg", 0, 0, 0, 0], ["00062", "CHAAT MASALA 1KG", "Masala & Spices", "pack", "1 kg", 0, 0, 0, 0], ["00063", "BUCKET 500GM ACHAR", "Achar & Chatni", "bucket", "500 g", 0, 0, 0, 0]];

// code, name, contact, phone, email, address, city
export const SEED_SUPPLIERS = [["00001", "REHAN AND BROTHERS", "", "0316-2539004", "", "", ""], ["00002", "AMEER ALI HASSAN", "", "0318-0034452", "", "", ""], ["00003", "SINDH ACHAR", "MR NAZEER", "0310-2731945", "", "", ""], ["00004", "MAMTA TEA", "MR SHARJEEL", "0313-1043192", "", "", ""], ["00005", "HINA TEA", "", "0301-2386446", "", "", ""]];

// name, contact, phone, email, address, city
export const SEED_CUSTOMERS = [];

// code, name, type, subtype, is_cash, system
export const CHART = [["1000", "Cash in Hand", "Asset", "Current Asset", 1, 1], ["1010", "Bank Account", "Asset", "Current Asset", 1, 1], ["1100", "Accounts Receivable", "Asset", "Current Asset", 0, 1], ["1200", "Inventory", "Asset", "Current Asset", 0, 1], ["1500", "Equipment", "Asset", "Fixed Asset", 0, 0], ["2000", "Accounts Payable", "Liability", "Current Liability", 0, 1], ["2100", "Tax Payable", "Liability", "Current Liability", 0, 1], ["3000", "Owner's Capital", "Equity", "", 0, 1], ["3900", "Retained Earnings", "Equity", "", 0, 1], ["4000", "Sales", "Income", "", 0, 1], ["4100", "Sales Discounts", "Income", "Contra", 0, 1], ["5000", "Cost of Goods Sold", "Expense", "Cost of Sales", 0, 1], ["6000", "Salaries & Wages", "Expense", "Operating", 0, 0], ["6100", "Rent", "Expense", "Operating", 0, 0], ["6200", "Utilities", "Expense", "Operating", 0, 0], ["6300", "Transport & Delivery", "Expense", "Operating", 0, 0], ["6400", "Packing & Supplies", "Expense", "Operating", 0, 0], ["6800", "Depreciation", "Expense", "Operating", 0, 1], ["6900", "Other Expenses", "Expense", "Operating", 0, 0], ["1590", "Accumulated Depreciation", "Asset", "Fixed Asset", 0, 1]];

export const PLACEHOLDER_LOGO_SVG = "<svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"0 0 200 200\">\n<rect width=\"200\" height=\"200\" rx=\"24\" fill=\"#d92d20\"/>\n<circle cx=\"100\" cy=\"100\" r=\"72\" fill=\"#e8ecb4\"/>\n<circle cx=\"100\" cy=\"100\" r=\"66\" fill=\"none\" stroke=\"#d92d20\" stroke-width=\"2\"\n        stroke-dasharray=\"7 7\"/>\n<text x=\"100\" y=\"104\" text-anchor=\"middle\" font-family=\"Georgia,serif\" font-size=\"52\"\n      font-weight=\"700\" fill=\"#c1121f\">UT</text>\n<text x=\"100\" y=\"132\" text-anchor=\"middle\" font-family=\"Helvetica,Arial,sans-serif\"\n      font-size=\"15\" letter-spacing=\"2.5\" fill=\"#7a1416\">TRADERS</text>\n</svg>";
